
// We can use timers to get the rotation to set parameters
/*
document.captureEvents(Event.MOUSEMOVE);
document.onmousemove = getCursorXY;


function getCursorXY(e) {
        if (document.getElementsByTagName('cursorx').length === 0) {
            document.body.appendChild(document.createElement('cursorx'));
        }   
        if (document.getElementsByTagName('cursory').length === 0) {
            document.body.appendChild(document.createElement('cursory'));
        }
        document.getElementsByTagName('cursorx')[0].setAttribute('value', event.clientX);
        document.getElementsByTagName('cursory')[0].setAttribute('value', event.clientY);
        
}

function getVal() {
        return document.getElementsByTagName('cursorx')[0].getAttribute('value') + document.getElementsByTagName('cursory')[0].getAttribute('value');
        
}
*/
